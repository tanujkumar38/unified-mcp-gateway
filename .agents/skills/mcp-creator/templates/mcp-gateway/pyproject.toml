[project]
name = "mcp-server-template-gateway"
version = "1.0.0"
description = "Context-optimized MCP Gateway implementing the Meta-Tool Pattern"
dependencies = [
    "mcp>=1.0.0",
    "pydantic>=2.0.0",
]
requires-python = ">=3.10"
